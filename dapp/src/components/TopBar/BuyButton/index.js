import React from 'react'

import {
    BuyButtonContainer
} from './styles'

const ConnectButton = (props) => {
    return (
        <BuyButtonContainer>
            buy $TTE
        </BuyButtonContainer>
    )
}

export default ConnectButton;
